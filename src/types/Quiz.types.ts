export enum Sort {
  "status" = "status",
  "updatedAt" = "updatedAt",
}

export enum Filter {
  "all" = "all",
  "draft" = "draft",
  "completed" = "completed",
  "favourite" = "favourite",
}

export enum Order {
  "asc" = "asc",
  "desc" = "desc",
}
